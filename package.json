{
  "name": "ciscospark-shipped",
  "version": "0.0.6",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp serve"
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
